---
name: terminate_contract_with_flash
description: "主动终止合同（违约解约）。"
version: 1.0
inputs:

tools:
  - terminate_contract_with_flash_tool
---

# Skill: terminate_contract_with_flash

## 何时使用
当需要执行 主动终止合同（违约解约）。 时使用此技能。

## 执行步骤
1. 调用 `terminate_contract_with_flash_tool` 工具。
2. 检查返回结果并根据需要反馈。

