---
name: relocate_ap
description: "迁移已有AP到新位置。visual_effect: "FLASH_THEN_DASHED""
version: 1.0
inputs:

tools:
  - relocate_ap_tool
---

# Skill: relocate_ap

## 何时使用
当需要执行 迁移已有AP到新位置。visual_effect: "FLASH_THEN_DASHED" 时使用此技能。

## 执行步骤
1. 调用 `relocate_ap_tool` 工具。
2. 检查返回结果并根据需要反馈。

