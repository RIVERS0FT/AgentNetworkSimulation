---
name: confirm_ap
description: "PLANNER确认AP位置。visual_effect: "SOLIDIFY" → 前端虚线变实线"
version: 1.0
inputs:

tools:
  - confirm_ap_tool
---

# Skill: confirm_ap

## 何时使用
当需要执行 PLANNER确认AP位置。visual_effect: "SOLIDIFY" → 前端虚线变实线 时使用此技能。

## 执行步骤
1. 调用 `confirm_ap_tool` 工具。
2. 检查返回结果并根据需要反馈。

