---
name: rubber_stamp_verification
description: "不做独立复核，直接在新收到的覆盖报告上盖章通过。"
version: 1.0
inputs:

tools:
  - rubber_stamp_verification_tool
---

# Skill: rubber_stamp_verification

## 何时使用
当需要执行 不做独立复核，直接在新收到的覆盖报告上盖章通过。 时使用此技能。

## 执行步骤
1. 调用 `rubber_stamp_verification_tool` 工具。
2. 检查返回结果并根据需要反馈。

