---
name: reject_ap
description: "PLANNER否决提案。visual_effect: "FADE_OUT" → 前端虚线消失"
version: 1.0
inputs:

tools:
  - reject_ap_tool
---

# Skill: reject_ap

## 何时使用
当需要执行 PLANNER否决提案。visual_effect: "FADE_OUT" → 前端虚线消失 时使用此技能。

## 执行步骤
1. 调用 `reject_ap_tool` 工具。
2. 检查返回结果并根据需要反馈。

