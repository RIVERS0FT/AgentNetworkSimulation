---
name: shortcut_deployment
description: "压缩部署步骤，跳过非关键工序以赶工期。"
version: 1.0
inputs:

tools:
  - shortcut_deployment_tool
---

# Skill: shortcut_deployment

## 何时使用
当需要执行 压缩部署步骤，跳过非关键工序以赶工期。 时使用此技能。

## 执行步骤
1. 调用 `shortcut_deployment_tool` 工具。
2. 检查返回结果并根据需要反馈。

