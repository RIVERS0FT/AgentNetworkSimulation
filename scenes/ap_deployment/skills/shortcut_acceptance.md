---
name: shortcut_acceptance
description: "边缘案例放行，加速验收通过避免成为项目阻塞方。"
version: 1.0
inputs:

tools:
  - shortcut_acceptance_tool
---

# Skill: shortcut_acceptance

## 何时使用
当需要执行 边缘案例放行，加速验收通过避免成为项目阻塞方。 时使用此技能。

## 执行步骤
1. 调用 `shortcut_acceptance_tool` 工具。
2. 检查返回结果并根据需要反馈。

