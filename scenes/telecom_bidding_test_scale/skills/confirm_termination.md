---
name: confirm_termination
description: "确认终止（动画完成后移除连线）。"
version: 1.0
inputs:

tools:
  - confirm_termination_tool
---

# Skill: confirm_termination

## 何时使用
当需要执行 确认终止（动画完成后移除连线）。 时使用此技能。

## 执行步骤
1. 调用 `confirm_termination_tool` 工具。
2. 检查返回结果并根据需要反馈。

